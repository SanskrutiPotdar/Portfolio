# Client — React + Vite

This folder contains a React + Vite client for Prashant Pitaliya's portfolio.

To run locally:

```bash
cd client
npm install
npm run dev
```

Place images in `client/src/assets/images` and PDFs in `client/src/assets/pdf` — the site auto-imports them.
